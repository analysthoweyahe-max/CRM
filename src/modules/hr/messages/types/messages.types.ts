import type { Channel, ChannelMemberResponse, LocalMessage } from 'stream-chat';

export type StreamChannel = Channel;
export type StreamMember  = ChannelMemberResponse;
export type StreamMessage = LocalMessage;
